import React from "react";
import styles from "scss/layout/ProfilePage.module.scss";

function ProfilePage() {
  return (
    <div>
      <header className={styles.header}>
        <h1 className="fs-22px">Account Settings</h1>
      </header>
    </div>
  );
}

export default ProfilePage;
